<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cqut.czb.bn.dao.mapper.directChargingSystem.PrepaidRefillMapperExtra" >
    <select id="getGoodsPrice" resultType="com.cqut.czb.bn.entity.entity.directChargingSystem.DirectChargingCommodity">
        SELECT
            commodity_id AS commodity_id,
            account AS account,
            discount AS discount,
            preferential_price AS preferentialPrice,
            state AS state,
            create_at AS createAt,
            update_at AS updateAt
        FROM
            czb_direct_charging_commodity
        <where>
            <if test="type != 0">
                state = #{type,jdbcType=INTEGER}
            </if>
        </where>
    </select>

    <select id="getInfoNum" parameterType="java.lang.String" resultType="com.cqut.czb.bn.entity.dto.directChargingSystem.UserCardRelationDto">
        SELECT
            sinopec_petrol_num,
            petrolChina_petrol_num,
            user_account
        FROM
            czb_user_card_relation
            LEFT JOIN czb_user ON czb_user_card_relation.user_id = czb_user.user_id
        WHERE
            czb_user_card_relation.user_id = #{userId,jdbcType=VARCHAR}
    </select>
</mapper>
